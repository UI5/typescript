# Supply-chain hardening — registry and auth only (pnpm 12+).
# All other settings live in pnpm-workspace.yaml.

# Explicit registry — prevents dependency confusion.
registry=https://registry.npmjs.org/
