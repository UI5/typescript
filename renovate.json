{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:best-practices"],
  "minimumReleaseAge": "7 days",
  "rangeStrategy": "pin",
  "vulnerabilityAlerts": {
    "enabled": true,
    "minimumReleaseAge": "3 days",
    "automerge": true,
    "automergeType": "pr",
    "labels": ["security"]
  },
  "lockFileMaintenance": {
    "enabled": true,
    "schedule": ["before 6am on monday"],
    "minimumReleaseAge": "0 days",
    "automerge": true,
    "automergeType": "pr"
  },
  "postUpdateOptions": ["pnpmDedupe"],
  "packageRules": [
    {
      "description": "Never pin peerDependencies — consumers need ranges",
      "matchDepTypes": ["peerDependencies"],
      "rangeStrategy": "widen"
    },
    {
      "description": "Never pin engines — CI controls runtime versions, not package.json",
      "matchDepTypes": ["engines"],
      "enabled": false
    },
    {
      "description": "Group all non-major updates to reduce PR noise",
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "non-major dependencies",
      "minimumReleaseAge": "7 days"
    },
    {
      "description": "Auto-merge minor and patch updates after age gate",
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "Never update: these versions are intentionally coupled to the UI5 version under test",
      "matchPackageNames": [
        "@openui5/*",
        "@types/openui5",
        "@types/jquery",
        "@types/qunit",
        "node"
      ],
      "enabled": false
    }
  ],
  "prCreation": "not-pending"
}
